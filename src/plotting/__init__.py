"""Plotting package.

`src.plotting.core` holds the shared machinery (palette, figure helpers, run
registries). `src.plotting.scripts` holds the entry points that build the paper
figures and tables.
"""
